PORT=5004
JWT_SECRET=change-this-secret
JWT_ISSUER=kites-local-backend
JWT_AUDIENCE=kites-mobile-app
OTP_TTL_MS=600000

# Email provider (Resend via HTTP API)
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxx
EMAIL_FROM=KITES <noreply@your-domain.com>

# Development only: include OTP in JSON responses
DEV_RETURN_OTP=false
